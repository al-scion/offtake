# App

This is the main app for the project. It is a React application that uses the Tanstack Router library for routing.

## Development

To start the development server, run:

```bash
bun run dev
```